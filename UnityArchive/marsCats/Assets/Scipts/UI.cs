using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UI : MonoBehaviour
{
    [SerializeField]
    private Slider h2O;

    [SerializeField]
    private Slider food;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Aqui metes lo que quieres hacer con los sliders. Los sliders son serialize field.y le cambias el .value
    }
}
